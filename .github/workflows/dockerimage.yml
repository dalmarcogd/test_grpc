name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: cd go_server
      run: cd go_server
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag test_grpc:$(date +%s)
